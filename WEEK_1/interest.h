// interest.h

#ifndef INTEREST_H
#define INTEREST_H

// Macro definitions for Simple Interest and Amount
#define SIMPLE_INTEREST(principal, rate, time) ((principal) * (rate) * (time) / 100)
#define AMOUNT(principal, interest) ((principal) + (interest))

#endif